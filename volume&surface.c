#include<stdio.h>
int main()
{
 int len,width,height;
 printf("enter the length,height,width of cuboid");
 scanf("%d%d%d",&len,&width,&height);
 volume=len*width*height;
 printf("the volume of cuboid is %d",volume);
 total surface=2*len+2*width+2*height;
 printf("the total surface area of cuboid is %d",total surface);
 return 0;
 }
