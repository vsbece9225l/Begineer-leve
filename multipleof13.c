#include<stdio.h>
int main()
{
  int num;
  printf("enter the number:");
  scanf("%d",&num);
  if(num%13==0)
    printf("the number is multiple of 13 %d",num);
    else
      printf("the number is not multiple of 13 %d",num);
      return o;
      }
      
